apiVersion: v1
kind: Service
metadata:
  name: pg-admin
  labels:
    app: pg-admin
    env: prod
  namespace: ic-webapp
spec:
  type: NodePort
  selector:
    app: pg-admin
  ports:
    - port: 5050
      protocol: TCP
      targetPort: 80
      nodePort: 30050