apiVersion: v1
kind: Service
metadata:
  name: odoo
  labels:
    app: odoo
    env: prod
  namespace: ic-webapp
spec:
  type: NodePort
  selector:
    app: odoo
  ports:
    - port: 8069
      protocol: TCP
      targetPort: 8069
      nodePort: 30069